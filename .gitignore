node_modules
dist
.env
.env.local
*.log
.DS_Store

.freebuff/
nul
*.tsbuildinfo

# Playwright throwaway output — fresh runs on every invocation.
test-results/
# Playwright HTML report generated under artifacts/ (kept; evidence preserved).
